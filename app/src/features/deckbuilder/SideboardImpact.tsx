import { useMemo, useState } from "react";
import type { Card, ClusterMatchupImpact } from "@gatcg/shared";
import CardImage from "../../components/CardImage";
import Panel from "../../components/ui/Panel";
import Section from "../../components/ui/Section";
import { FUNCTIONAL_ROLE_LABELS, functionalRoles, type FunctionalRole } from "./functionalCopies";
import { probabilityAtLeast } from "./synergyReadiness";
import { matchupSideboardRecommendations, suggestedMatchupCut } from "./matchupSideboard";

const ROLES = Object.keys(FUNCTIONAL_ROLE_LABELS) as FunctionalRole[];

export default function SideboardImpact({ mainLines, sideboardLines, catalogByName, matchups = [] }: {
  mainLines: { name: string; quantity: number }[];
  sideboardLines: { name: string; quantity: number }[];
  catalogByName: Map<string, Card>;
  matchups?: ClusterMatchupImpact[];
}) {
  const [outName, setOutName] = useState("");
  const [inName, setInName] = useState("");
  const [opponentId, setOpponentId] = useState("");
  const deckSize = mainLines.reduce((sum, line) => sum + line.quantity, 0);
  const mainCopies = useMemo(() => new Map(mainLines.map((line) => [line.name, line.quantity])), [mainLines]);
  const roleCopies = useMemo(() => new Map(ROLES.map((role) => [role, mainLines.reduce((sum, line) => {
    const card = catalogByName.get(line.name);
    return sum + (card && functionalRoles(card).includes(role) ? line.quantity : 0);
  }, 0)])), [mainLines, catalogByName]);
  const outCard = catalogByName.get(outName);
  const inCard = catalogByName.get(inName);
  const outRoles = outCard ? functionalRoles(outCard) : [];
  const inRoles = inCard ? functionalRoles(inCard) : [];
  const ready = Boolean(outCard && inCard && deckSize > 0);
  const incomingBefore = mainCopies.get(inName) ?? 0;
  const incomingAfter = incomingBefore + 1;
  const twoBefore = probabilityAtLeast(deckSize, incomingBefore, Math.min(10, deckSize), 2);
  const twoAfter = probabilityAtLeast(deckSize, incomingAfter, Math.min(10, deckSize), 2);
  const oneBefore = probabilityAtLeast(deckSize, incomingBefore, Math.min(10, deckSize), 1);
  const oneAfter = probabilityAtLeast(deckSize, incomingAfter, Math.min(10, deckSize), 1);
  const selectedMatchup = matchups.find((matchup) => matchup.opponentClusterId === (opponentId || matchups[0]?.opponentClusterId));
  const recommendations = useMemo(() => selectedMatchup ? matchupSideboardRecommendations(selectedMatchup, new Set(sideboardLines.map((line) => line.name))) : [], [selectedMatchup, sideboardLines]);

  function previewRecommendation(cardName: string) {
    setInName(cardName);
    if (selectedMatchup) setOutName(suggestedMatchupCut(mainLines, selectedMatchup, cardName));
  }

  if (sideboardLines.length === 0) return null;
  return <Panel data-component="SideboardImpact" className="mt-4 shadow-sm"><Section heading="dense" title="Matchup sideboard plan" description="Scan every sideboard card first, then preview a one-copy swap.">{selectedMatchup && <div className="mt-3 flex flex-wrap items-center gap-2 rounded-lg border border-ctp-surface1 bg-ctp-base/35 p-3"><label className="text-xs text-ctp-subtext0">Opponent <select value={opponentId || matchups[0]?.opponentClusterId || ""} onChange={(event) => setOpponentId(event.target.value)} className="ml-1 min-h-11 rounded-md border border-ctp-surface1 bg-ctp-mantle px-2 text-xs text-ctp-text">{matchups.map((matchup) => <option key={matchup.opponentClusterId} value={matchup.opponentClusterId}>{matchup.opponentClusterName} ({matchup.games}g)</option>)}</select></label><span className={`text-xs font-semibold ${selectedMatchup.baselineWinRate >= 0.5 ? "text-ctp-green" : "text-ctp-red"}`}>{(selectedMatchup.baselineWinRate * 100).toFixed(0)}% historical win rate</span></div>}<div className="mt-3"><div className="flex items-end justify-between gap-2"><div><h4 className="text-sm font-semibold text-ctp-text">Sideboard at a glance</h4><p className="mt-0.5 text-[10px] text-ctp-subtext0">Select a card to preview it. Cost and roles come from its printed card data.</p></div><span className="text-[10px] text-ctp-subtext0">{sideboardLines.reduce((sum, line) => sum + line.quantity, 0)} cards</span></div><div className="mt-2 grid grid-cols-2 gap-2 sm:grid-cols-3 lg:grid-cols-4">{sideboardLines.map((line) => { const card = catalogByName.get(line.name); const roles = card ? functionalRoles(card) : []; const evidence = recommendations.find((item) => item.cardName === line.name); return <button key={line.name} type="button" onClick={() => previewRecommendation(line.name)} aria-pressed={inName === line.name} className={`min-h-11 rounded-xl border p-2 text-left transition-colors ${inName === line.name ? "border-ctp-blue bg-ctp-blue/10" : "border-ctp-surface1 bg-ctp-base/35 hover:border-ctp-blue/60"}`}><div className="flex gap-2"><div className="relative aspect-[5/7] w-12 shrink-0 overflow-hidden rounded-md bg-ctp-surface0">{card?.editions[0] && <CardImage image={card.editions[0].image} alt="" className="h-full w-full object-cover" />}<span className="absolute right-0.5 top-0.5 rounded-full bg-ctp-base/90 px-1 text-[9px] font-semibold text-ctp-text">{line.quantity}×</span></div><div className="min-w-0"><p className="line-clamp-2 text-xs font-semibold text-ctp-text">{line.name}</p><p className="mt-0.5 text-[10px] text-ctp-subtext0">Reserve {card?.cost_reserve ?? 0}</p><div className="mt-1 flex flex-wrap gap-1">{roles.length > 0 ? roles.slice(0, 2).map((role) => <span key={role} className="rounded-full bg-ctp-surface0 px-1.5 py-0.5 text-[9px] text-ctp-subtext1">{FUNCTIONAL_ROLE_LABELS[role]}</span>) : <span className="text-[9px] text-ctp-subtext0">No detected role</span>}</div>{evidence && <p className="mt-1 text-[9px] font-medium text-ctp-green">{evidence.answers[0] ? `Answer signal +${(evidence.answers[0].mitigation * 100).toFixed(0)} pts` : `Helpful signal +${(evidence.helpfulLift * 100).toFixed(0)} pts`}</p>}</div></div></button>; })}</div>{selectedMatchup && recommendations.length === 0 && <p className="mt-2 text-xs text-ctp-subtext0">No card currently in this sideboard has qualifying positive evidence for this matchup.</p>}</div><details className="mt-3 rounded-lg border border-ctp-surface1 bg-ctp-base/25 p-3"><summary className="flex min-h-11 cursor-pointer items-center text-xs font-medium text-ctp-blue">Fine-tune the one-copy swap</summary><div className="mt-2 grid gap-3 sm:grid-cols-2"><label className="text-xs text-ctp-subtext0">Move out of Main<select value={outName} onChange={(event) => setOutName(event.target.value)} className="mt-1 block min-h-11 w-full rounded-lg border border-ctp-surface1 bg-ctp-mantle px-3 py-2 text-sm text-ctp-text"><option value="">Choose a card…</option>{mainLines.map((line) => <option key={line.name} value={line.name}>{line.name} ({line.quantity})</option>)}</select></label><label className="text-xs text-ctp-subtext0">Move in from Sideboard<select value={inName} onChange={(event) => setInName(event.target.value)} className="mt-1 block min-h-11 w-full rounded-lg border border-ctp-surface1 bg-ctp-mantle px-3 py-2 text-sm text-ctp-text"><option value="">Choose a card…</option>{sideboardLines.map((line) => <option key={line.name} value={line.name}>{line.name} ({line.quantity})</option>)}</select></label></div></details>{inCard && !outCard && <p className="mt-3 rounded-lg border border-ctp-blue/30 bg-ctp-blue/5 p-3 text-xs text-ctp-subtext1">Selected <span className="font-semibold text-ctp-text">{inName}</span>. Open “Fine-tune” to choose the Main Deck card it replaces.</p>}{ready && <><div className="mt-3 rounded-lg border border-ctp-blue/30 bg-ctp-blue/5 p-2.5 text-xs"><span className="text-ctp-subtext0">See incoming card by 10:</span> <span className="font-semibold tabular-nums text-ctp-teal">{(oneBefore * 100).toFixed(1)}% → {(oneAfter * 100).toFixed(1)}%</span> <span className="text-ctp-green">(+{((oneAfter - oneBefore) * 100).toFixed(1)} pts)</span></div><div className="mt-2 grid grid-cols-2 gap-2 lg:grid-cols-4">{ROLES.map((role) => { const before = roleCopies.get(role) ?? 0; const after = before - (outRoles.includes(role) ? 1 : 0) + (inRoles.includes(role) ? 1 : 0); const beforeOdds = probabilityAtLeast(deckSize, before, Math.min(10, deckSize), 1); const afterOdds = probabilityAtLeast(deckSize, after, Math.min(10, deckSize), 1); const delta = afterOdds - beforeOdds; return <div key={role} className="rounded-lg border border-ctp-surface1 p-2.5"><div className="text-[10px] uppercase tracking-wide text-ctp-subtext0">{FUNCTIONAL_ROLE_LABELS[role]}</div><div className="mt-1 text-sm font-semibold tabular-nums text-ctp-text">{before} → {after} copies</div><div className={`text-xs tabular-nums ${delta > 0 ? "text-ctp-green" : delta < 0 ? "text-ctp-red" : "text-ctp-subtext0"}`}>{delta === 0 ? "No odds change" : `${delta > 0 ? "+" : ""}${(delta * 100).toFixed(1)} pts by 10`}</div></div>; })}</div><div className="mt-2 rounded-lg border border-ctp-surface1 p-2.5 text-xs"><span className="text-ctp-subtext0">Incoming-card clumping:</span> <span className="font-medium text-ctp-text">2+ by 10 changes from {(twoBefore * 100).toFixed(1)}% to {(twoAfter * 100).toFixed(1)}%</span></div><p className="mt-2 text-[10px] leading-4 text-ctp-subtext0">Recommendations are correlations from recorded games, not causal guarantees. “Champion-wide” answers use the broader Champion matchup when the exact named-build sample was too small. Suggested cuts are provisional high-quantity cards without positive matchup evidence; preview assumes one copy each and never changes the deck.</p></>}</Section></Panel>;
}
