import { useMemo, useState } from "react";
import type { Card } from "@gatcg/shared";
import { computeGamePlanViews, type GamePlanRole } from "../../lib/gamePlanReadiness";
import { inferStartingHandSize, naturalCardsSeenByTurn, type PlayOrder } from "../../lib/turnToPlay";
import { computeStageDrawQuality } from "../../lib/stageDrawQuality";
import type { StageUsefulness } from "../../lib/analysisProfile";

interface Line { name: string; quantity: number }
const LABEL: Record<GamePlanRole, string> = { enabler: "Setup", payoff: "Payoff", protection: "Protection" };
const COLOR: Record<GamePlanRole, string> = { enabler: "text-ctp-blue", payoff: "text-ctp-mauve", protection: "text-ctp-green" };

type PlanView = "readiness" | "timing" | "stages" | "affordability";

export default function GamePlanReadiness({ mainLines, materialLines, catalogByName, sharedAssignments, sharedStageUsefulness = {}, sharedEffectiveCosts = {}, onSharedAssignmentsChange, planName = "Primary plan", onPlanNameChange }: { mainLines: Line[]; materialLines: Line[]; catalogByName: Map<string, Card>; sharedAssignments?: Record<string, GamePlanRole | "">; sharedStageUsefulness?: Record<string, StageUsefulness | "">; sharedEffectiveCosts?: Record<string, number>; onSharedAssignmentsChange?: (assignments: Record<string, GamePlanRole | "">) => void; planName?: string; onPlanNameChange?: (name: string) => void }) {
  const [localAssignments, setLocalAssignments] = useState<Record<string, GamePlanRole | "">>({});
  const assignments = sharedAssignments ?? localAssignments;
  const setAssignments = onSharedAssignmentsChange ?? setLocalAssignments;
  const updateAssignment = (name: string, role: GamePlanRole | "") => setAssignments({ ...assignments, [name]: role });
  const [required, setRequired] = useState<Record<GamePlanRole, number>>({ enabler: 1, payoff: 1, protection: 1 });
  const [targetTurn, setTargetTurn] = useState(3);
  const [setupTurn, setSetupTurn] = useState(2);
  const [maximumEarlyPayoffs, setMaximumEarlyPayoffs] = useState(1);
  const [view, setView] = useState<PlanView>("readiness");
  const [playOrder, setPlayOrder] = useState<PlayOrder>("first");
  const [query, setQuery] = useState("");
  const deckSize = mainLines.reduce((sum, line) => sum + line.quantity, 0);
  const opening = inferStartingHandSize(materialLines, catalogByName);
  const roleLines = useMemo(() => ({
    enabler: mainLines.filter((line) => assignments[line.name] === "enabler"),
    payoff: mainLines.filter((line) => assignments[line.name] === "payoff"),
    protection: mainLines.filter((line) => assignments[line.name] === "protection"),
  }), [assignments, mainLines]);
  const roles = (Object.keys(roleLines) as GamePlanRole[]).map((role) => ({ role, copies: roleLines[role].reduce((sum, line) => sum + line.quantity, 0), required: required[role] }));
  const planViews = computeGamePlanViews(deckSize, opening, roles, setupTurn, targetTurn, playOrder, maximumEarlyPayoffs);
  const result = planViews.readiness;
  const engineResult = planViews.timing;
  const cardsSeenAtTarget = Math.min(deckSize, naturalCardsSeenByTurn(targetTurn, opening, playOrder));
  const costFor = (name: string): number | null => {
    const override = sharedEffectiveCosts[name];
    if (Number.isFinite(override)) return Math.max(0, Math.floor(override));
    const printed = catalogByName.get(name)?.cost_reserve;
    return printed != null && printed >= 0 ? printed : null;
  };
  const individuallyAffordable = (name: string) => { const cost = costFor(name); return cost != null && cost + 1 <= cardsSeenAtTarget; };
  const affordableRoles = (Object.keys(roleLines) as GamePlanRole[]).map((role) => ({ role, copies: roleLines[role].filter((line) => individuallyAffordable(line.name)).reduce((sum, line) => sum + line.quantity, 0), required: required[role] }));
  const affordabilityResult = computeGamePlanViews(deckSize, opening, affordableRoles, setupTurn, targetTurn, playOrder, maximumEarlyPayoffs).readiness;
  const costBlocked = mainLines.filter((line) => assignments[line.name] && !individuallyAffordable(line.name)).map((line) => ({ ...line, cost: costFor(line.name) }));
  const stageCopies = (stage: StageUsefulness) => mainLines.filter((line) => (sharedStageUsefulness[line.name] || (assignments[line.name] === "enabler" ? "early" : assignments[line.name] === "payoff" ? "late" : assignments[line.name] === "protection" ? "flexible" : "")) === stage).reduce((sum, line) => sum + line.quantity, 0);
  const stageResult = Object.values(sharedStageUsefulness).some(Boolean) ? computeStageDrawQuality(deckSize, opening, { earlyCopies: stageCopies("early"), lateCopies: stageCopies("late"), flexibleCopies: stageCopies("flexible"), conditionalCopies: stageCopies("conditional") }, Math.min(setupTurn, targetTurn), targetTurn, playOrder, maximumEarlyPayoffs) : planViews.stages;
  const filtered = mainLines.filter((line) => line.name.toLowerCase().includes(query.trim().toLowerCase()));
  const ready = roleLines.enabler.length > 0 && roleLines.payoff.length > 0;
  const percent = (value: number) => `${(value * 100).toFixed(1)}%`;

  return <section data-component="GamePlanReadiness" className="pt-3">
    <div className="flex flex-wrap items-start justify-between gap-3"><div><h3 className="font-semibold text-ctp-text">Game Plan Readiness</h3><p className="mt-1 max-w-2xl text-xs text-ctp-subtext0">One shared plan, viewed as same-turn readiness, staged setup-to-payoff timing, or draw quality by game stage. Cards remain in disjoint roles so the probability models do not double-count a physical card.</p>{sharedAssignments && <p className="mt-1 text-[10px] font-medium text-ctp-blue">Using the shared Prepare Analysis profile.</p>}</div><button type="button" onClick={() => setAssignments({})} disabled={!Object.values(assignments).some(Boolean)} className="text-xs font-medium text-ctp-subtext1 hover:text-ctp-text disabled:opacity-40">Clear roles</button></div>
    <label className="mt-3 block max-w-sm text-xs text-ctp-subtext0">Plan name<input value={planName} onChange={(event) => onPlanNameChange?.(event.target.value)} disabled={!onPlanNameChange} maxLength={80} className="mt-1 min-h-11 w-full rounded-lg border border-ctp-surface1 bg-ctp-base px-3 text-sm font-medium text-ctp-text disabled:opacity-70" /></label>
    <div className="mt-3 flex gap-1 overflow-x-auto rounded-lg border border-ctp-surface1 bg-ctp-base/40 p-1" role="tablist" aria-label="Game plan analysis view">{([{ key: "readiness", label: "Readiness" }, { key: "timing", label: "Timing" }, { key: "stages", label: "Stage draws" }, { key: "affordability", label: "Affordability" }] as const).map((item) => <button key={item.key} type="button" role="tab" aria-selected={view === item.key} onClick={() => setView(item.key)} className={`min-h-10 whitespace-nowrap rounded-md px-3 text-xs font-semibold ${view === item.key ? "bg-ctp-blue text-ctp-base" : "text-ctp-subtext1 hover:bg-ctp-surface0"}`}>{item.label}</button>)}</div>
    <div className="mt-4 grid gap-3 lg:grid-cols-[minmax(0,1.2fr)_minmax(18rem,0.8fr)]">
      <div><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Filter cards…" aria-label="Filter deck cards" className="w-full rounded-md border border-ctp-surface1 bg-ctp-base px-3 py-2 text-sm text-ctp-text" /><div className="mt-2 max-h-96 overflow-y-auto rounded-lg border border-ctp-surface1">{filtered.map((line) => <div key={line.name} className="grid grid-cols-[minmax(0,1fr)_8.5rem] items-center gap-2 border-b border-ctp-surface0 px-3 py-2 last:border-0"><div className="min-w-0"><p className="truncate text-sm text-ctp-text">{line.name}</p><p className="text-[10px] text-ctp-subtext0">{line.quantity} copies</p></div><select value={assignments[line.name] ?? ""} onChange={(event) => updateAssignment(line.name, event.target.value as GamePlanRole | "")} aria-label={`Role for ${line.name}`} className="rounded-md border border-ctp-surface1 bg-ctp-mantle px-2 py-1.5 text-xs text-ctp-text"><option value="">Unassigned</option><option value="enabler">Setup</option><option value="payoff">Payoff</option><option value="protection">Protection</option></select></div>)}</div></div>
      <div className="space-y-3">
        <div className="grid grid-cols-3 gap-2">{(Object.keys(roleLines) as GamePlanRole[]).map((role) => { const copies = roles.find((input) => input.role === role)!.copies; return <label key={role} className="rounded-lg border border-ctp-surface1 bg-ctp-base/40 p-2 text-xs"><span className={`block font-semibold ${COLOR[role]}`}>{LABEL[role]}</span><span className="mt-1 block text-lg font-bold tabular-nums text-ctp-text">{copies}</span><span className="mt-1 flex items-center gap-1 text-[10px] text-ctp-subtext0">Need <select value={required[role]} onChange={(event) => setRequired((current) => ({ ...current, [role]: Number(event.target.value) }))} aria-label={`${LABEL[role]} cards required`} className="rounded border border-ctp-surface1 bg-ctp-mantle px-1 py-0.5 text-xs text-ctp-text">{[1,2,3,4].map((value) => <option key={value}>{value}</option>)}</select></span></label>; })}</div>
        <div className="grid grid-cols-2 gap-2"><label className="text-xs text-ctp-subtext0">Target turn<select value={targetTurn} onChange={(event) => setTargetTurn(Number(event.target.value))} className="mt-1 block w-full rounded-md border border-ctp-surface1 bg-ctp-base px-2 py-2 text-sm text-ctp-text">{[1,2,3,4,5,6,7,8].map((turn) => <option key={turn} value={turn}>Turn {turn}</option>)}</select></label><label className="text-xs text-ctp-subtext0">Play order<select value={playOrder} onChange={(event) => setPlayOrder(event.target.value as PlayOrder)} className="mt-1 block w-full rounded-md border border-ctp-surface1 bg-ctp-base px-2 py-2 text-sm text-ctp-text"><option value="first">Going first</option><option value="second">Going second</option></select></label></div>
        {!ready ? <div className="rounded-lg border border-ctp-blue/30 bg-ctp-blue/5 p-3 text-sm text-ctp-subtext1">Assign at least one Setup and one Payoff to calculate the plan.</div> : view === "readiness" ? <><div className="grid grid-cols-2 gap-2"><Metric label={`Core by turn ${targetTurn}`} value={percent(result.targetCoreProbability)} /><Metric label="With protection" value={result.targetProtectedProbability == null ? "Optional" : percent(result.targetProtectedProbability)} /></div><div className="rounded-lg border border-ctp-surface1 bg-ctp-base/40 p-3 text-xs text-ctp-subtext1"><p><b className="text-ctp-text">Bottleneck:</b> {result.bottleneck ? LABEL[result.bottleneck] : "—"}</p><p className="mt-1"><b className="text-ctp-text">Payoff without setup:</b> {percent(result.payoffWithoutEnabler)}</p><p className="mt-1"><b className="text-ctp-text">Setup without payoff:</b> {percent(result.enablerWithoutPayoff)}</p>{result.expectedCoreCardsSeen != null && <p className="mt-1"><b className="text-ctp-text">Expected access:</b> {result.expectedCoreCardsSeen.toFixed(1)} cards seen</p>}{result.bestAdditionalCopy && <p className="mt-2 text-ctp-green">Best extra role copy: {LABEL[result.bestAdditionalCopy.role]} (+{(result.bestAdditionalCopy.gain * 100).toFixed(1)} points by turn {targetTurn})</p>}</div></> : view === "timing" ? <><label className="text-xs text-ctp-subtext0">Setup deadline<select value={Math.min(setupTurn, targetTurn)} onChange={(event) => setSetupTurn(Number(event.target.value))} className="mt-1 block min-h-11 w-full rounded-lg border border-ctp-surface1 bg-ctp-base px-3 text-sm text-ctp-text">{Array.from({ length: targetTurn }, (_, index) => index + 1).map((turn) => <option key={turn} value={turn}>Turn {turn}</option>)}</select></label><div className="grid grid-cols-2 gap-2"><Metric label="Plan online" value={percent(engineResult.onlineProbability)} /><Metric label="Payoff given setup" value={percent(engineResult.payoffGivenSetup)} /><Metric label="Stranded payoff" value={percent(engineResult.payoffStrandedProbability)} /><Metric label="Unused setup" value={percent(engineResult.setupUnusedProbability)} /></div><p className="rounded-lg border border-ctp-surface1 bg-ctp-base/40 p-3 text-xs text-ctp-subtext1">Setup must be found by turn {Math.min(setupTurn, targetTurn)}; Payoff may arrive through turn {targetTurn}. Ratio bottleneck: <b className="text-ctp-text">{engineResult.bottleneck === "producer" ? "Setup" : engineResult.bottleneck === "payoff" ? "Payoff" : "Balanced"}</b>.</p></> : view === "stages" ? <><label className="text-xs text-ctp-subtext0">Maximum Payoff cards in the early window<select value={maximumEarlyPayoffs} onChange={(event) => setMaximumEarlyPayoffs(Number(event.target.value))} className="mt-1 block min-h-11 w-full rounded-lg border border-ctp-surface1 bg-ctp-base px-3 text-sm text-ctp-text">{[0,1,2,3,4].map((value) => <option key={value}>{value}</option>)}</select></label><div className="grid grid-cols-2 gap-2"><Metric label="Early functional" value={percent(stageResult.openingFunctional)} /><Metric label="Late injection" value={percent(stageResult.lateInjection)} /><Metric label="Both stages" value={percent(stageResult.stagedPlan)} /><Metric label="Early payoff clog" value={percent(stageResult.earlyClog)} /></div><p className="rounded-lg border border-ctp-surface1 bg-ctp-base/40 p-3 text-xs text-ctp-subtext1">Saved stage usefulness overrides the role defaults: Setup is early, Payoff late, and Protection flexible. “Both stages” requires early function plus a fresh late-window useful draw.</p></> : <><div className="grid grid-cols-2 gap-2"><Metric label={`Raw core access · T${targetTurn}`} value={percent(result.targetCoreProbability)} /><Metric label="Access + individual cost" value={percent(affordabilityResult.targetCoreProbability)} /></div><div className="rounded-lg border border-ctp-surface1 bg-ctp-base/40 p-3 text-xs text-ctp-subtext1"><p>By turn {targetTurn}, the natural ceiling is {cardsSeenAtTarget} cards seen. A role card qualifies when its effective Reserve cost plus the card itself fits that ceiling.</p>{costBlocked.length > 0 ? <p className="mt-2 text-ctp-yellow"><b>Outside this ceiling:</b> {costBlocked.map((line) => `${line.name} (${line.cost == null ? "unknown cost" : `R${line.cost}`})`).join(", ")}.</p> : <p className="mt-2 text-ctp-green">Every assigned role card fits the individual payment ceiling.</p>}</div><p className="text-[11px] leading-4 text-ctp-subtext0">This bounds each selected card independently. It does not claim the full Setup/Payoff sequence can be paid together, retain prior cards, or satisfy reduction conditions. Edit shared effective costs in Resource timing.</p></>}
      </div>
    </div>
    {ready && view === "readiness" && <div className="mt-4 overflow-x-auto"><table className="w-full min-w-[34rem] text-left text-xs"><thead className="text-ctp-subtext0"><tr><th className="px-2 py-1.5">Turn</th>{result.points.map((point) => <th key={point.turn} className="px-2 py-1.5 text-right">{point.turn}</th>)}</tr></thead><tbody><tr className="border-t border-ctp-surface1"><th className="px-2 py-2 font-medium text-ctp-text">Core ready</th>{result.points.map((point) => <td key={point.turn} className="px-2 py-2 text-right tabular-nums text-ctp-mauve">{percent(point.coreProbability)}</td>)}</tr><tr className="border-t border-ctp-surface0"><th className="px-2 py-2 font-medium text-ctp-text">Protected plan</th>{result.points.map((point) => <td key={point.turn} className="px-2 py-2 text-right tabular-nums text-ctp-green">{point.protectedProbability == null ? "—" : percent(point.protectedProbability)}</td>)}</tr><tr className="border-t border-ctp-surface0 text-ctp-subtext0"><th className="px-2 py-2 font-medium">Cards seen</th>{result.points.map((point) => <td key={point.turn} className="px-2 py-2 text-right tabular-nums">{point.seen}</td>)}</tr></tbody></table></div>}
    <p className="mt-3 text-[11px] leading-4 text-ctp-subtext0">Exact without-replacement access odds using natural draws and the detected opening hand. Costs, activation legality, sequencing, board state, draw effects, and opponent responses are not modeled.</p>
  </section>;
}

function Metric({ label, value }: { label: string; value: string }) { return <div className="rounded-lg border border-ctp-surface1 bg-ctp-base/40 p-3"><p className="text-[10px] uppercase tracking-wide text-ctp-subtext0">{label}</p><p className="mt-1 text-xl font-bold tabular-nums text-ctp-text">{value}</p></div>; }
